---
- name: gitlab-ce
  docker_container:
    name: gitlab-ce
    image: gitlab/gitlab-ce:latest
    restart: yes
    state: started
#   hostname: 'gitlab.example.com'
    env:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://{{ extip }}'
    ports:
      - "80:80"
      - "443:443"
      - "2222:22"
    volumes:
      - '/srv/gitlab/config:/etc/gitlab'
      - '/srv/gitlab/logs:/var/log/gitlab'
      - '/srv/gitlab/data:/var/opt/gitlab'
